# qdrant-memory

Demonstrates registering a custom memory backend that implements `MemoryBackendProtocol`; all methods are stubs that log intended Qdrant operations so the example runs without installing `qdrant-client`.

Configure `qdrant_url` and `collection_name` to match your deployment when you replace the stubs.

