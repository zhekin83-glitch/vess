interface:
  display_name: Update Todo Step
  short_description: Update the status of a plan step
  default_prompt: Use $update-todo-step
i18n:
  zh:
    name: 更新计划步骤
    description: 更新计划中某个步骤的状态，每完成一步后必须调用以跟踪进度。

