interface:
  display_name: Deliver Artifacts
  short_description: Deliver artifacts (files/images/voice) to current IM chat via...
  default_prompt: Use $deliver-artifacts
i18n:
  zh:
    name: 发送文件
    description: 通过 IM 网关发送文件、图片或语音到当前聊天会话，返回发送回执。

