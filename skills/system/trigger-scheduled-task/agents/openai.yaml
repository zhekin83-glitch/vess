interface:
  display_name: Trigger Scheduled Task
  short_description: Immediately trigger scheduled task without waiting for schedu...
  default_prompt: Use $trigger-scheduled-task
i18n:
  zh:
    name: 立即触发任务
    description: 立即执行定时任务，无需等待预定时间，用于测试或提前运行。

