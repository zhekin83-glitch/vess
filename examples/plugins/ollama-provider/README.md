# ollama-provider

Example plugin that registers a stub `ollama_native` LLM provider and an `ollama` vendor registry backed by Ollama’s `/api/tags` endpoint. Use `config_schema.json` to point `ollama_url` at your daemon.

