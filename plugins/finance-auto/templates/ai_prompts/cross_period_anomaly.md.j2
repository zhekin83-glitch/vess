你是注册会计师，正在做跨期波动分析。下面是同比变化 > 50% 的报表项（已按量级 + 增长率 % 脱敏，仅有项目名 + 比率，无原始金额）。

输入异常项：
$safe_payload_json

请按风险等级排序，并对每个异常项给出：
- 可能原因（如季节性 / 业务结构变化 / 关联交易 / 收入确认时点 / 政策性变动）
- 建议向业务部门询问的问题清单（具体到部门 + 问题）

输出 JSON 数组，每个元素：
- item_name: 项目名
- yoy_pct: 同比 %（保留作为字符串）
- risk_level: "高" / "中" / "低"
- possible_reasons: 字符串数组
- questions_to_ask: 字符串数组
