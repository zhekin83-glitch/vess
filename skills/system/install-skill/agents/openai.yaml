interface:
  display_name: Install Skill
  short_description: Install skill from URL or Git repository to local skills/ dir...
  default_prompt: Use $install-skill
i18n:
  zh:
    name: 安装技能
    description: 从 URL 或 Git 仓库安装技能到本地 skills/ 目录，支持 GitHub 简写格式。

