interface:
  display_name: Desktop Wait
  short_description: Wait for UI element or window to appear
  default_prompt: Use $desktop-wait
i18n:
  zh:
    name: 等待元素
    description: 等待 UI 元素或窗口出现，用于等待对话框打开、加载完成或同步应用状态。

