# echo-channel

Registers an `echo` IM channel type with a stub `EchoAdapter` that logs operations instead of talking to a real network.

Demonstrates `api.register_channel` and adapter factory wiring.

