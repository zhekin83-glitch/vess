{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Lark CLI Tool Configuration",
  "type": "object",
  "properties": {
    "lark_cli_path": {
      "type": "string",
      "default": "lark-cli",
      "description": "Path to the lark-cli executable. Use 'lark-cli' if installed globally via npm."
    },
    "npx_path": {
      "type": "string",
      "default": "npx",
      "description": "Path to npx (used for installation and skill management)."
    },
    "default_timeout": {
      "type": "integer",
      "default": 30,
      "minimum": 5,
      "maximum": 300,
      "description": "Default command timeout in seconds."
    },
    "default_format": {
      "type": "string",
      "default": "json",
      "enum": ["json", "pretty", "table", "ndjson", "csv"],
      "description": "Default output format for lark-cli commands."
    },
    "default_identity": {
      "type": "string",
      "default": "user",
      "enum": ["user", "bot"],
      "description": "Default identity (--as user or --as bot) for commands."
    }
  },
  "required": []
}

