interface:
  display_name: Browser Open
  short_description: Launch browser or check its status
  default_prompt: Use $browser-open
i18n:
  zh:
    name: 打开浏览器
    description: 启动浏览器或检查其状态，返回当前状态信息（是否打开、URL、标题、标签数）。

