你是中国会计软件识别专家。下面给出一份余额表前几行的字段名 / 列结构 / 表头特征（已脱敏，仅含字段名与类型，不含金额或客户名）。

请识别该余额表来自哪一款 ERP / 财务软件，并按以下候选给出最可能的归属：

候选列表：用友 (UFIDA) / 金蝶 (Kingdee) / 管家婆 (Top Soft) / 浪潮 (Inspur) / 通用 / 未知

输入元数据（schema only）：
$safe_payload_json

请用 JSON 格式输出，包含以下字段：
- erp_source: 候选列表中的一项
- confidence: 0.0 - 1.0 的置信度
- evidence: 1-3 条字段级证据（如「列 7-8 为期初借/期初贷，符合用友 T3 风格」）
