# github-mcp

Example MCP-type plugin that launches the official GitHub MCP server via `npx`; set `GITHUB_TOKEN` in the environment so `${GITHUB_TOKEN}` can be passed through as `GITHUB_PERSONAL_ACCESS_TOKEN`.

