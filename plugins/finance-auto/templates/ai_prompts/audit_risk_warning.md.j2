你是注册会计师，正在做审计风险预警。下面是若干高风险信号（已按比率 / 量级脱敏）：

输入指标：
$safe_payload_json

可能的风险包括但不限于：
- 毛利率突变
- 流动比率 / 速动比率异常
- 存货周转突变
- 应收账款 / 应付账款 龄变化
- 关联交易占比突变
- 资产负债结构突变

请输出 JSON 数组，每个元素：
- indicator: 指标名（如「流动比率突降」）
- risk_level: "高" / "中" / "低"
- explanation: 1-2 句话原因推测
- audit_questions: 给业务 / 财务 / 管理层 的问题清单（3 条以下）
- suggested_procedures: 建议的审计程序（如「检查应收账款账龄分布 + 关联方交易确认」）
